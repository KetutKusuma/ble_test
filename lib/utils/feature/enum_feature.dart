enum FormType { text, int, float, listselect, bool, setid, date }

// enum FormSelectType{

// }

enum FeatureType {
  get,
  set,
  dosomething,
}

enum ReturnType { string, int8, int16, int32, bool, float }

enum Role { admin, operator, guest, none }
